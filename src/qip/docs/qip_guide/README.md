# QIP Guide (LaTeX)

This folder contains a LaTeX guide that mirrors the module structure.

Build with:
- Overleaf, or
- local LaTeX toolchain

Entry point: `main.tex`.

The guide is intended to explain the API boundaries and the math conventions.
